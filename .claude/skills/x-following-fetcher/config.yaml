# X Following Fetcher Configuration
feed_url: "https://www.bestblogs.dev/feeds/rss?type=twitter"

# Number of tweets to fetch
count: 20

# Output settings
output:
  # Maximum characters per tweet content
  max_content_length: 150000
  # Include tweet metrics (likes, retweets, etc.)
  include_metrics: true
